using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Equipment : MonoBehaviour
{
    /*
    void Equiped()
    {
        Item HelmetSlot =   new Item { itemType = Item.ItemType.Helmet, amount = 1 };
        Item ChestSlot =    new Item { itemType = Item.ItemType.Chest,  amount = 1 };
        Item LegSlot =      new Item { itemType = Item.ItemType.Legs, amount = 1 };
        Item MainHandSlot = new Item { itemType = Item.ItemType.Sword, amount = 1 };
        Item OffHandSlot =  new Item { itemType = Item.ItemType.Shield, amount = 1 };
        Item BootsSlot =    new Item { itemType = Item.ItemType.Boots, amount = 1 };
    }


    public void EquipItem(Item item, int slot)
    {
        if (item.itemType = Item.ItemType.Helmet)
        {
            if (HelmetSlot.ItemType = Item.ItemType.Empty)
            {

            }
        }
        //OnItemListChanged?.Invoke(this, EventArgs.Empty);
        //Debug.Log(itemList.Count);
    }



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
